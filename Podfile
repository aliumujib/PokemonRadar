# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'PokemonRadar' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PokemonRadar
  #pod 'Firebase', '>= 2.5.1'
  pod 'Mapbox-iOS-SDK', '~> 3.5'
  pod 'GooglePlaces'
  pod 'Alamofire'
  pod 'Xia'
  pod 'MapboxNavigation', '~> 0.4.0'
  pod 'Parse'

  #Tried to use firebase ... but issues with adding pods so I got tired
  #pod 'GeoFire', '>= 1.1'
  #pod 'Firebase/Core'
  #pod 'Firebase/Database'

end
